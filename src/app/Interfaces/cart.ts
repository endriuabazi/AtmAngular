export interface Cart {

    
        id: string;
        cvv: number; 
        cartNumber: string;
        name: string,
        surname: string,
        currency: string;
        balance: number;
    
    
}
